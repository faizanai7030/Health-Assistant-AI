kind = "web"
previewPath = "/"
title = "Clinic WhatsApp AI Agent"
version = "1.0.0"
id = "artifacts/clinic-agent"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23726

[services.development]
run = "pnpm --filter @workspace/clinic-agent run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clinic-agent", "run", "build" ]
publicDir = "artifacts/clinic-agent/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23726"
BASE_PATH = "/"
